{
  "servers": {
    "convex-mcp-dev": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y", 
        "convex@latest", 
        "mcp", 
        "start", 
        "--project-dir", 
        "${workspaceFolder}",
        "--disable-production-deployments"
      ]
    }
  }
}
